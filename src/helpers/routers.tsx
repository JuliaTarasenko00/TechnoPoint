const base = "/TechnoPoint";

export const path = {
  home: base,
  about: `${base}/about`,
  wishlist: `${base}/wishlist`,
  cart: `${base}/cart`,
  product: `${base}/product`,
  auth: `${base}/auth`,
};
